import React from 'react';

interface BookingCalendarProps {
  name: string;
  price: number;
}

const BookingCalendar = ({ name, price }: BookingCalendarProps) => {
  return <div>BookingCalendar</div>;
};

export default BookingCalendar;
